import { tv } from "tailwind-variants";

export const WEEK_ITEM_VARIANTS = tv({
  base: "relative mx-[6px] grid place-items-center text-xs text-gray first:text-red last:text-blue",
  variants: {},
});

export const WEEK_ITEM_SIZE = {
  width: 50,
  height: 44,
};
